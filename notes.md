# remember you cant return in a forEach
    - use a for loop instead

# ...args
    - function areThereDuplicates(...args) {
        console.log(args);
    }
    - areThereDuplicates(1,2,3) => [1,2,3]

# spilce is just for arr and how does it work?
